services:
  pricing-agent-rag:
    build: .
    container_name: pricing_agent_rag
    ports:
      - "8501:8501"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - rag_knowledge_db:/app/pricing_knowledge_db
    restart: unless-stopped

volumes:
  rag_knowledge_db:
